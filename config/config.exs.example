import Config

config :bot,
  guild_id: DISCORD_SERVER_ID_HERE

config :nostrum,
  token: "DISCORD_BOT_TOKEN_HERE"

config :logger, :console,
  format: "$time $metadata[$level] $levelpad$message\n",
  level: :info
